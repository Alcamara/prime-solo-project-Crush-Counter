export  const snackbar = (state = {open: false, vertical: 'top', horizontal: 'center'}, action) => {
    // switch (action.type) {
    //     case "BOOKMARK_TOURNAMENT":
            
    //         return state = {open: true, vertical: 'top', horizontal: 'center', message: ''} ;
    
    //     default:
    //         return state;
    // }

}